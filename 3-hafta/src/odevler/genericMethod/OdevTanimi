Bir generic metot (printArray) yazınız ve bu metot,
verilen farklı veri türlerinden (Integer, String, Double vs.) oluşan dizileri ekrana yazdırsın.

Örnek Kullanım:

Integer[] intArray = {1, 2, 3, 4, 5};

String[] strArray = {"Java", "generics", "Ödev"};

printArray(intArray);

printArray(strArray);
Beklenen Çıktı:

1 2 3 4 5